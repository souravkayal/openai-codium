# ASP.NET Core TODO App

Simple CRUD TODO application built with ASP.NET Core MVC and SQLite.

## Run

```bash
dotnet restore
dotnet run --project TodoApp/TodoApp.csproj
```

Then open: `http://localhost:5000` (or the port shown in console).
